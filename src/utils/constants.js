/**
 * Global constants for the macOS defaults application
 */

// Delimiter used to separate domain and key in setting IDs
// Using $ instead of . to avoid conflicts with domains that contain dots
export const SETTINGS_DELIMITER = '$';

// UI Constants
export const MAX_SETTINGS_PER_CATEGORY = 20;